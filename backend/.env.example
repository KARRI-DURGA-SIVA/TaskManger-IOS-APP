# Never commit real AWS credentials. For production, use an IAM role instead.
AWS_REGION=ap-southeast-2
S3_BUCKET=taskmanager-user-files-durgasiva
AWS_ACCESS_KEY_ID=replace-with-a-new-key
AWS_SECRET_ACCESS_KEY=replace-with-a-new-secret
